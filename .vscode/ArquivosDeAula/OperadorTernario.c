#include <stdio.h>

int main(){
    int idade = 18;
    int resultado;

    // Operador Ternário 
    // Condição ex idade > 20 ? código caso a condição seja verdadeira (ex: printf("verdadeira")) : código caso a condição seja falsa (ex: printf("falsa"))

    idade >= 18 ? printf("Voce e maior de idade\n") : printf("Voce e menor de idade\n");
}